set-api-key.sh
*.key
# Mojang's published mappings may be used but NOT redistributed complete and unmodified
# (their own licence header says so). Only tables DERIVED from them ship in this repo.
mappings-*.txt
*-mappings.txt
client.txt
server.txt
rules.json
